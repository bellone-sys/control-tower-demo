/**
 * Versione dell'applicazione.
 * Aggiornare questo valore ad ogni rilascio in produzione,
 * allineandolo con il tag Git (es. v0.3.0).
 *
 * Il componente VersionBadge.jsx mostra questa versione in tutta l'app.
 * Ricorda di aggiornare sia questa costante che i file HTML statici in public/
 * per mantenere coerenza tra React app e documentazione.
 */
export const APP_VERSION = '0.30.0'
